        # Pre-flight Checklist — Module 14

        Before starting:

        - [ ] On branch `module/14-repl` (run `git status`)
        - [ ] Module `13-grep-tool` is green: `runner test --module 13`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 14` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
