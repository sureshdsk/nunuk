        # Pre-flight Checklist — Module 10

        Before starting:

        - [ ] On branch `module/10-read-tool-with-pagination` (run `git status`)
        - [ ] Module `09-bash-tool` is green: `runner test --module 09`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 10` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
