        # Pre-flight Checklist — Module 03

        Before starting:

        - [ ] On branch `module/03-streaming` (run `git status`)
        - [ ] Module `02-system-prompt-and-args` is green: `runner test --module 02`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 03` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
