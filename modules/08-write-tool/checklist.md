        # Pre-flight Checklist — Module 08

        Before starting:

        - [ ] On branch `module/08-write-tool` (run `git status`)
        - [ ] Module `07-the-agent-loop` is green: `runner test --module 07`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 08` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
