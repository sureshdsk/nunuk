        # Pre-flight Checklist — Module 29

        Before starting:

        - [ ] On branch `module/29-persistent-session-memory` (run `git status`)
        - [ ] Module `28-project-memory` is green: `runner test --module 28`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 29` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
