        # Pre-flight Checklist — Module 34

        Before starting:

        - [ ] On branch `module/34-mcp-client` (run `git status`)
        - [ ] Module `33-hooks` is green: `runner test --module 33`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 34` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
