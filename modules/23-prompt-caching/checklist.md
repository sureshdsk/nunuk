        # Pre-flight Checklist — Module 23

        Before starting:

        - [ ] On branch `module/23-prompt-caching` (run `git status`)
        - [ ] Module `22-token-counting` is green: `runner test --module 22`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 23` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
