        # Pre-flight Checklist — Module 16

        Before starting:

        - [ ] On branch `module/16-diff-display` (run `git status`)
        - [ ] Module `15-markdown-render` is green: `runner test --module 15`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 16` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
