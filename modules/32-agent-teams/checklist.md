# Pre-flight Checklist — Module 32

Before starting:

- [ ] On branch `module/32-agent-teams` (run `git status`)
- [ ] Module `31-subagents-task-tool` is green: `runner test --module 31`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
- [ ] `runner doctor` passes

Before submitting:

- [ ] All assertions in `runner test --module 32` are green
- [ ] You can articulate the trade-off you made in this module
- [ ] (Optional) Tried the agent against a real LLM at least once
