# Pre-flight Checklist — Module 25

Before starting:

- [ ] On branch `module/25-structured-output` (run `git status`)
- [ ] Module `24-provider-abstraction` is green: `runner test --module 24`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
- [ ] `runner doctor` passes

Before submitting:

- [ ] All assertions in `runner test --module 25` are green
- [ ] You can articulate the trade-off you made in this module
- [ ] (Optional) Tried the agent against a real LLM at least once
