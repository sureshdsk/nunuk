        # Pre-flight Checklist — Module 26

        Before starting:

        - [ ] On branch `module/26-todo-tool` (run `git status`)
        - [ ] Module `25-structured-output` is green: `runner test --module 25`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 26` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
