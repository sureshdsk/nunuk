        # Pre-flight Checklist — Module 13

        Before starting:

        - [ ] On branch `module/13-grep-tool` (run `git status`)
        - [ ] Module `12-glob-tool` is green: `runner test --module 12`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 13` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
