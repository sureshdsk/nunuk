        # Pre-flight Checklist — Module 11

        Before starting:

        - [ ] On branch `module/11-edit-tool` (run `git status`)
        - [ ] Module `10-read-tool-with-pagination` is green: `runner test --module 10`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 11` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
