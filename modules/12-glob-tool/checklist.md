        # Pre-flight Checklist — Module 12

        Before starting:

        - [ ] On branch `module/12-glob-tool` (run `git status`)
        - [ ] Module `11-edit-tool` is green: `runner test --module 11`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 12` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
