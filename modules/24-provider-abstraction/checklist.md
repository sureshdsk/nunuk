        # Pre-flight Checklist — Module 24

        Before starting:

        - [ ] On branch `module/24-provider-abstraction` (run `git status`)
        - [ ] Module `23-prompt-caching` is green: `runner test --module 23`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 24` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
