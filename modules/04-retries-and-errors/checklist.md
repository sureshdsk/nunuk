        # Pre-flight Checklist — Module 04

        Before starting:

        - [ ] On branch `module/04-retries-and-errors` (run `git status`)
        - [ ] Module `03-streaming` is green: `runner test --module 03`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 04` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
