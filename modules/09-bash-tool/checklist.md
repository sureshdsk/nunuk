        # Pre-flight Checklist — Module 09

        Before starting:

        - [ ] On branch `module/09-bash-tool` (run `git status`)
        - [ ] Module `08-write-tool` is green: `runner test --module 08`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 09` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
