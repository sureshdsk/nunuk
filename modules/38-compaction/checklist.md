        # Pre-flight Checklist — Module 38

        Before starting:

        - [ ] On branch `module/38-compaction` (run `git status`)
        - [ ] Module `37-cost-tracking` is green: `runner test --module 37`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 38` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
