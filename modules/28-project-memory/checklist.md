        # Pre-flight Checklist — Module 28

        Before starting:

        - [ ] On branch `module/28-project-memory` (run `git status`)
        - [ ] Module `27-plan-mode` is green: `runner test --module 27`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 28` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
