        # Pre-flight Checklist — Module 35

        Before starting:

        - [ ] On branch `module/35-mcp-server` (run `git status`)
        - [ ] Module `34-mcp-client` is green: `runner test --module 34`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 35` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
