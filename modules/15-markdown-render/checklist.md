        # Pre-flight Checklist — Module 15

        Before starting:

        - [ ] On branch `module/15-markdown-render` (run `git status`)
        - [ ] Module `14-repl` is green: `runner test --module 14`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 15` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
