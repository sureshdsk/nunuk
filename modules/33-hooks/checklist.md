        # Pre-flight Checklist — Module 33

        Before starting:

        - [ ] On branch `module/33-hooks` (run `git status`)
        - [ ] Module `32-agent-teams` is green: `runner test --module 32`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 33` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
