        # Pre-flight Checklist — Module 07

        Before starting:

        - [ ] On branch `module/07-the-agent-loop` (run `git status`)
        - [ ] Module `06-execute-tools` is green: `runner test --module 06`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 07` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
