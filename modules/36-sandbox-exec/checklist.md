        # Pre-flight Checklist — Module 36

        Before starting:

        - [ ] On branch `module/36-sandbox-exec` (run `git status`)
        - [ ] Module `35-mcp-server` is green: `runner test --module 35`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 36` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
