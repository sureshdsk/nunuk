        # Pre-flight Checklist — Module 17

        Before starting:

        - [ ] On branch `module/17-approvals` (run `git status`)
        - [ ] Module `16-diff-display` is green: `runner test --module 16`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 17` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
