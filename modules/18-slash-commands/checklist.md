        # Pre-flight Checklist — Module 18

        Before starting:

        - [ ] On branch `module/18-slash-commands` (run `git status`)
        - [ ] Module `17-approvals` is green: `runner test --module 17`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 18` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
