# Pre-flight Checklist — Module 21

Before starting:

- [ ] On branch `module/21-stuck-loop-detection` (run `git status`)
- [ ] Module `20-history-truncation` is green: `runner test --module 20`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
- [ ] `runner doctor` passes

Before submitting:

- [ ] All assertions in `runner test --module 21` are green
- [ ] You can articulate the trade-off you made in this module
- [ ] (Optional) Tried the agent against a real LLM at least once
