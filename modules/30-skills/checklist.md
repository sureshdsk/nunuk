# Pre-flight Checklist — Module 30

Before starting:

- [ ] On branch `module/30-skills` (run `git status`)
- [ ] Module `29-persistent-session-memory` is green: `runner test --module 29`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
- [ ] `runner doctor` passes

Before submitting:

- [ ] All assertions in `runner test --module 30` are green
- [ ] You can articulate the trade-off you made in this module
- [ ] (Optional) Tried the agent against a real LLM at least once
