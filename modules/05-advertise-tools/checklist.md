        # Pre-flight Checklist — Module 05

        Before starting:

        - [ ] On branch `module/05-advertise-tools` (run `git status`)
        - [ ] Module `04-retries-and-errors` is green: `runner test --module 04`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 05` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
