        # Pre-flight Checklist — Module 39

        Before starting:

        - [ ] On branch `module/39-capstone` (run `git status`)
        - [ ] Module `38-compaction` is green: `runner test --module 38`
- [ ] `OPENROUTER_API_KEY` is set (or `.env` is sourced)
        - [ ] `runner doctor` passes

        Before submitting:

        - [ ] All assertions in `runner test --module 39` are green
        - [ ] You can articulate the trade-off you made in this module
        - [ ] (Optional) Tried the agent against a real LLM at least once
